# Trunk touch-ups carried from landings (one small fixer, fresh branch from main)

Delete this file in that change's landing commit. Filed 2026-09-17 on the PC
session. None of these blocks anything; batch them into one change once the
licence change worker/114e5b3f5b1b has landed, so the criterion (e) wording
matches its CLAUDE.md Licence section.

## RESOURCES_EXPLOITATION.md (landed from worker/76bbf2823a53)

- Round-9 W1 :1394-1399 — the verification log quotes the two converter
  pages' footer lines exactly, but the bullet says both were "fetched the
  same way and the same day" as the 2026-09-15 reading, which recorded the
  wrong string. Add "(footer wording re-verified 2026-09-16)" beside the
  quoted lines. The caveat at :155-157 needs nothing.
- Round-9 W2 :1223 — `AFQ_SPLIT` is never expanded; AF (aggression factor)
  is explained at :30-31 but AFQ is aggression *frequency*, a different
  statistic. Expand once at :1223 or gloss beside AF in the plain-words
  section.
- Criterion (e) :134-138 and §1.6's AGPL discussion rest on "this project
  never hands it to anybody". Repository is public and GPL-3.0. Restate the
  criterion as the licence change does for RESOURCES_BOTS.md; re-read each
  (e) reason; change a verdict only where the premise changes it.
