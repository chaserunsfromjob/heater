# Project rules: <project>

Wins over `rules/global.md` where the two conflict. Carry only what is true of
this project and false elsewhere.

## Commands

- Run the full suite with `<command>`.
- Run the gate with `<command>`.
- Deploy with `<command>`.

## Irreversible surfaces

- <Name each surface this project declares irreversible, one per bullet.>

## Domain

- <Carry project-specific rules here, one imperative bullet each.>
